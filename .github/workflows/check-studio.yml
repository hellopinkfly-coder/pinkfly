# Is the current schema actually in the deployed Studio?
name: Check studio

on:
  push:
    paths:
      - "scripts/check-studio.ts"
      - ".github/workflows/check-studio.yml"
  workflow_dispatch:
    inputs:
      site_url:
        description: "Deployment to check"
        required: false
        default: "https://pinkfly.vercel.app"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check:studio -- "${{ inputs.site_url || 'https://pinkfly.vercel.app' }}"
